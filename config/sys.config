[
 {kernel, [
  {logger_level, info},

  {logger, [
    %% Console
    {handler, default, logger_std_h,
       #{level => warning}
    },

    %% Disk logger
    {handler, disk_log, logger_disk_log_h,
        #{config => #{
            file => "log/app.log",
            type => wrap,
            max_no_files => 10,
            max_no_bytes => 1048576000
         },
         level => notice,
         filter_default => log,
         formatter => {flatlog, #{
           map_depth => 3,
           term_depth => 50
         }}
        }
    }
  ]}
 ]},

  %% cuter configuration:
 { cuter, [{python_path, "/usr/bin/python2.7"}]}
].
